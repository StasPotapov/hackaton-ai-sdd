{
  "name": "gigaspec-pdf-build",
  "version": "1.0.0",
  "private": true,
  "description": "Сборка PDF-артефактов email-package/ из деки (HTML) и PROPOSAL.md",
  "scripts": {
    "build": "npm run build:proposal && npm run build:deck",
    "build:deck": "node build-deck-pdf.js",
    "build:proposal": "./build-proposal-pdf.sh"
  },
  "dependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
